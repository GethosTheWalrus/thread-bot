name: threadbot
description: "A thread-based ChatGPT clone backed by Temporal"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.12.0-113.0.dev <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  intl: ^0.19.0
  flutter_markdown: ^0.7.1
  url_launcher: ^6.3.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
